import cli from '../../src/cli/check-routes.js';
import {
  expect,
} from 'chai';
import 'mocha';

describe('check-routes', () => {
  it('should be a function', () => {
    expect(cli,).to.be.a('function',);
  },);
},);
