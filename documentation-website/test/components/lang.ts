import {
  Lang,
} from '../../src/components/lang.jsx';
import {
  expect,
} from 'chai';

describe('components/lang', () => {
  it('should be a function', () => {
    expect(Lang,).to.be.a('function',);
  },);
},);
