import executor from '../src/executor';
import {
  expect,
} from 'chai';
import 'mocha';

describe('executor', () => {
  it('should be a function', () => {
    expect(executor,).to.be.a('function',);
  },);
},);
