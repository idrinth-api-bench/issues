import make from '../src/make.js';
import {
  expect,
} from 'chai';
import 'mocha';

describe('make', () => {
  it('should be a function', () => {
    expect(make,).to.be.a('function',);
  },);
},);
